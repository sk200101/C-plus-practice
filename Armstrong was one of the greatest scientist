#include <iostream>
using namespace std;
int main()
{
    int number,r,sum,temp;
    cin>>number;
    temp=number;
    while(number>0)
{
r=number%10;
sum=sum+(rrr);
number=number/10;
}
if(temp==sum)
cout<<"Part of Memorable Coin"<<endl;
else
cout<<"Not a Part of Memorable Coin"<<endl; 
    return 0;
}
